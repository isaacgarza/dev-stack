###############################################################
#  dev-stack: commands.yaml                                    #
#-------------------------------------------------------------#
#  Single source of truth for dev-stack CLI commands & flags.  #
#                                                             #
#  - Used by scripts/generate_docs.py to auto-generate         #
#    docs/reference.md                                         #
#  - Update this file when adding or changing commands         #
#    in setup.sh or manage.sh                                  #
#  - Do NOT manually edit docs/reference.md (auto-generated)   #
###############################################################

setup:
  - --init
  - --services
  - --project
  - --cleanup-existing
  - --connect-existing
  - --force
  - --dry-run
  - --skip-validation
  - --validate-only
  - --debug
  - --config
  - --list-services
  - --interactive

manage:
  - start
  - stop
  - restart
  - info
  - status
  - logs
  - connect
  - exec
  - backup
  - restore
  - update
  - cleanup
  - list-all
  - cleanup-all
  - monitor
  - scale
  - cp
  - help
