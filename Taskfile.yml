version: "3"

# ============================================================================
# VARIABLES
# ============================================================================
vars:
  BINARY_NAME: dev-stack
  BUILD_DIR: build
  MODULE_PATH: github.com/isaacgarza/dev-stack
  VERSION:
    sh: git describe --tags --always --dirty 2>/dev/null || echo "dev"
  COMMIT:
    sh: git rev-parse --short HEAD 2>/dev/null || echo "unknown"
  BUILD_DATE:
    sh: date -u +"%Y-%m-%dT%H:%M:%SZ"
  LDFLAGS: '-ldflags "-X {{.MODULE_PATH}}/internal/cli.version={{.VERSION}} -X {{.MODULE_PATH}}/internal/cli.commit={{.COMMIT}} -X {{.MODULE_PATH}}/internal/cli.date={{.BUILD_DATE}}"'
  GOLANGCI_LINT_VERSION: "v2.4.0"

# ============================================================================
# TASKS
# ============================================================================
tasks:
  # ----------------------------------------------------------------------------
  # DEFAULT TASK
  # ----------------------------------------------------------------------------
  default:
    desc: "🚀 Build the binary (default task)"
    silent: true
    cmds:
      - task: build

  # ----------------------------------------------------------------------------
  # BUILD TASKS
  # ----------------------------------------------------------------------------
  build:
    desc: "🔨 Build the Go binary for current platform"
    deps: [deps]
    sources:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
    generates:
      - "{{.BUILD_DIR}}/{{.BINARY_NAME}}"
    silent: true
    cmds:
      - echo "🔨 Building {{.BINARY_NAME}}..."
      - mkdir -p {{.BUILD_DIR}}
      - go build {{.LDFLAGS}} -o {{.BUILD_DIR}}/{{.BINARY_NAME}} ./cmd/dev-stack
      - echo "✅ Built at {{.BUILD_DIR}}/{{.BINARY_NAME}}"

  build-all:
    desc: "🌍 Build binaries for all supported platforms"
    deps: [deps]
    silent: true
    cmds:
      - echo "🌍 Building {{.BINARY_NAME}} for all platforms..."
      - mkdir -p {{.BUILD_DIR}}
      - GOOS=linux GOARCH=amd64 go build {{.LDFLAGS}} -o {{.BUILD_DIR}}/{{.BINARY_NAME}}-linux-amd64 ./cmd/dev-stack
      - GOOS=linux GOARCH=arm64 go build {{.LDFLAGS}} -o {{.BUILD_DIR}}/{{.BINARY_NAME}}-linux-arm64 ./cmd/dev-stack
      - GOOS=darwin GOARCH=amd64 go build {{.LDFLAGS}} -o {{.BUILD_DIR}}/{{.BINARY_NAME}}-darwin-amd64 ./cmd/dev-stack
      - GOOS=darwin GOARCH=arm64 go build {{.LDFLAGS}} -o {{.BUILD_DIR}}/{{.BINARY_NAME}}-darwin-arm64 ./cmd/dev-stack
      - GOOS=windows GOARCH=amd64 go build {{.LDFLAGS}} -o {{.BUILD_DIR}}/{{.BINARY_NAME}}-windows-amd64.exe ./cmd/dev-stack
      - echo "✅ All binaries built in {{.BUILD_DIR}}/"

  install:
    desc: "📦 Install the binary to GOPATH/bin"
    deps: [build]
    silent: true
    cmds:
      - echo "📦 Installing {{.BINARY_NAME}}..."
      - go install {{.LDFLAGS}} ./cmd/dev-stack
      - echo "✅ Installed to $(go env GOPATH)/bin/{{.BINARY_NAME}}"

  # ----------------------------------------------------------------------------
  # DEVELOPMENT TASKS
  # ----------------------------------------------------------------------------
  dev:
    desc: "🚀 Quick start - build and show help"
    deps: [build]
    silent: true
    cmds:
      - echo "🚀 Running {{.BINARY_NAME}} in development mode..."
      - ./{{.BUILD_DIR}}/{{.BINARY_NAME}} --help

  watch:
    desc: "👀 Watch for changes and rebuild automatically"
    watch: true
    sources:
      - "**/*.go"
    silent: true
    cmds:
      - echo "👀 Rebuilding due to changes..."
      - task: build

  # ----------------------------------------------------------------------------
  # DEPENDENCY MANAGEMENT
  # ----------------------------------------------------------------------------
  deps:
    desc: "📥 Download and tidy Go dependencies"
    silent: true
    cmds:
      - echo "📥 Downloading Go dependencies..."
      - go mod download
      - go mod tidy
      - echo "✅ Dependencies updated"

  # ----------------------------------------------------------------------------
  # GIT AUTOMATION
  # ----------------------------------------------------------------------------
  commit:
    desc: "🤖 Generate AI-powered commit message based on staged changes"
    silent: true
    cmds:
      - |
        # Check if there are staged changes
        STAGED_FILES=$(git diff --cached --name-only)
        if [ -z "$STAGED_FILES" ]; then
          echo "❌ No staged changes found. Use 'git add' first."
          exit 1
        fi

        echo "🤖 Analyzing staged changes with AI..."

        # Get detailed diff for AI analysis
        DIFF_CONTENT=$(git diff --cached --no-color)
        FILE_LIST=$(echo "$STAGED_FILES" | tr '\n' ', ' | sed 's/,$//')
        
        # Create AI prompt for commit message generation
        AI_PROMPT="Analyze these Git changes and generate a conventional commit message.

        Files changed: $FILE_LIST

        Diff content:
        $DIFF_CONTENT

        Generate a conventional commit message following this format:
        <type>(<scope>): <description>

        Types: feat, fix, docs, style, refactor, test, chore, ci, build, perf
        Keep description under 50 characters, be specific and clear.
        Only return the commit message, nothing else."

        # Use AI to generate commit message (requires q CLI or similar)
        if command -v q >/dev/null 2>&1; then
          echo "🤖 Generating commit message..."
          COMMIT_MSG=$(echo "$AI_PROMPT" | q chat --no-interactive)
          
          # Clean up the response (remove quotes, extra whitespace)
          COMMIT_MSG=$(echo "$COMMIT_MSG" | sed 's/^["'"'"']//;s/["'"'"']$//' | xargs)
          
        else
          echo "⚠️  AI not available, falling back to pattern-based analysis..."
          # Fallback to existing logic
          TYPE="feat"
          DESCRIPTION="improve codebase"
          
          # Analyze file types for fallback
          if echo "$STAGED_FILES" | grep -q "_test\.go"; then
            TYPE="test"
            DESCRIPTION="add/update tests"
          elif echo "$STAGED_FILES" | grep -q "\.md$"; then
            TYPE="docs"
            DESCRIPTION="update documentation"
          elif echo "$STAGED_FILES" | grep -q "Taskfile\.yml\|\.github/"; then
            TYPE="ci"
            DESCRIPTION="improve build automation"
          elif echo "$STAGED_FILES" | grep -q "\.go$"; then
            TYPE="feat"
            DESCRIPTION="add features"
          fi
          
          COMMIT_MSG="$TYPE: $DESCRIPTION"
        fi

        # Show what will be committed
        echo ""
        echo "🔍 Staged files:"
        echo "$STAGED_FILES" | sed 's/^/  /'
        echo ""
        echo "📝 Generated commit message:"
        echo "  $COMMIT_MSG"
        echo ""

        # Commit with generated message
        git commit -m "$COMMIT_MSG"
        echo "✅ Committed successfully!"

  # === GIT AUTOMATION ===
  create-pr:
    desc: "🔀 Create a pull request with auto-generated title and description based on branch changes"
    silent: true
    cmds:
      - |
        BRANCH=$(git branch --show-current)
        if [ "$BRANCH" = "main" ]; then
          echo "❌ Cannot create PR from main branch"
          exit 1
        fi

  create-pr:
    desc: "🤖 Create AI-powered pull request with smart title and description"
    silent: true
    cmds:
      - |
        BRANCH=$(git branch --show-current)
        if [ "$BRANCH" = "main" ]; then
          echo "❌ Cannot create PR from main branch"
          exit 1
        fi

        echo "🤖 Analyzing branch changes with AI: $BRANCH"

        # Get comprehensive change analysis
        COMMITS=$(git log --oneline main..$BRANCH)
        COMMIT_COUNT=$(git rev-list --count main..$BRANCH)
        CHANGED_FILES=$(git diff --name-only main..$BRANCH)
        FULL_DIFF=$(git diff main..$BRANCH --no-color)
        
        # Create AI prompt for PR generation
        AI_PROMPT="Analyze these Git branch changes and generate a professional pull request title and description.

        Branch: $BRANCH
        Commits: $COMMIT_COUNT
        
        Recent commits:
        $COMMITS
        
        Changed files:
        $CHANGED_FILES
        
        Full diff:
        $FULL_DIFF
        
        Generate:
        1. A concise PR title (under 60 chars) following conventional commits format
        2. A comprehensive PR description with:
           - Summary of changes
           - Key features/fixes
           - Files changed breakdown
           - Testing checklist
        
        Format as:
        TITLE: <title>
        DESCRIPTION:
        <description>"

        # Use AI to generate PR content
        if command -v q >/dev/null 2>&1; then
          echo "🤖 Generating PR title and description..."
          AI_RESPONSE=$(echo "$AI_PROMPT" | q chat --no-interactive)
          
          # Extract title and description
          TITLE=$(echo "$AI_RESPONSE" | grep "^TITLE:" | sed 's/^TITLE: //' | xargs)
          DESCRIPTION=$(echo "$AI_RESPONSE" | sed -n '/^DESCRIPTION:/,$ p' | sed '1d')
          
          # Fallback if parsing fails
          if [ -z "$TITLE" ]; then
            TITLE="feat: improve codebase"
          fi
          if [ -z "$DESCRIPTION" ]; then
            DESCRIPTION="## Changes Summary\n\nBranch: $BRANCH\nCommits: $COMMIT_COUNT\n\n### Files Changed\n$CHANGED_FILES"
          fi
          
        else
          echo "⚠️  AI not available, falling back to template-based generation..."
          
          # Fallback title generation
          TITLE="feat: "
          if echo "$CHANGED_FILES" | grep -q "_test\.go"; then
            TITLE="${TITLE}add tests and "
          fi
          if echo "$CHANGED_FILES" | grep -q "\.go" | grep -v "_test\.go"; then
            TITLE="${TITLE}improve core functionality and "
          fi
          if echo "$COMMITS" | grep -qi "fix\|bug"; then
            TITLE="${TITLE}fix issues and "
          fi
          TITLE="${TITLE}enhance codebase"
          
          # Fallback description
          DESCRIPTION="## 🚀 Changes Summary

        **Branch:** \`$BRANCH\`  
        **Commits:** $COMMIT_COUNT

        ### 📝 Recent Commits
        \`\`\`
        $COMMITS
        \`\`\`

        ### 📁 Files Changed (\$(echo \"$CHANGED_FILES\" | wc -l) files)
        \`\`\`
        $CHANGED_FILES
        \`\`\`

        ## 🧪 Testing Checklist
        - [ ] All tests pass
        - [ ] No breaking changes  
        - [ ] Documentation updated if needed
        - [ ] Code follows project standards"
        fi

        echo ""
        echo "📝 Generated PR Title:"
        echo "  $TITLE"
        echo ""
        echo "📄 Generated Description:"
        echo "$DESCRIPTION" | head -10
        echo "..."
        echo ""

        # Create PR
        gh pr create \
          --title "$TITLE" \
          --body "$DESCRIPTION" \
          --assignee @me

        echo "✅ Pull request created successfully!"

  push-and-pr:
    desc: "🚀 Push current branch and create PR in one command"
    silent: true
    cmds:
      - |
        BRANCH=$(git branch --show-current)
        echo "🚀 Pushing branch: $BRANCH"
        git push -u origin $BRANCH
      - task: create-pr

  pre-commit:
    desc: "🔒 Run all pre-commit checks"
    silent: true
    cmds:
      - echo "🔒 Running pre-commit checks..."
      - task: pre-commit-fix
      - task: pre-commit-verify

  pre-commit-fix:
    desc: "🔧 Auto-fix formatting and module issues"
    silent: true
    cmds:
      - echo "🔧 Auto-fixing formatting and modules..."
      - task: fmt
      - go mod tidy

  pre-commit-verify:
    desc: "✅ Verify all checks pass after fixes"
    silent: true
    cmds:
      - |
        echo "✅ Verifying fixes..."
        UNSTAGED_CHANGES=$(git diff --name-only)
        if [ -n "$UNSTAGED_CHANGES" ]; then
          echo ""
          echo "❌ Pre-commit failed: Files were modified during formatting/tidy"
          echo ""
          echo "📋 Modified files:"
          git diff --name-only
          echo ""
          echo "🔧 Please add these changes and commit again:"
          echo "   git add ."
          echo "   git commit"
          echo ""
          exit 1
        fi
      - task: lint
      - echo "✅ Pre-commit checks passed"

  pre-push:
    desc: "🚀 Run all pre-push checks"
    silent: true
    cmds:
      - echo "🚀 Running pre-push checks..."
      - task: test
      - task: test-coverage-check
      - task: build
      - task: docs-lastmod-update
      - echo "✅ Pre-push checks passed"

  fmt-check:
    desc: "📝 Check Go code formatting"
    silent: true
    cmds:
      - |
        if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
          echo "❌ Code is not formatted. Run: task fmt"
          gofmt -s -l .
          exit 1
        fi
        echo "✅ Code formatting OK"

  mod-tidy-check:
    desc: "📦 Check if go.mod is tidy"
    silent: true
    cmds:
      - go mod tidy
      - |
        if ! git diff --exit-code go.mod go.sum; then
          echo "❌ go.mod/go.sum not tidy. Run: go mod tidy"
          exit 1
        fi
        echo "✅ Go modules are tidy"

  lint-md:
    desc: "📝 Lint markdown files"
    silent: true
    cmds:
      - echo "📝 Linting markdown files..."
      - cd docs-site && npm run lint:md
      - echo "✅ Markdown linting passed"

  docs-sync-check:
    desc: "📚 Check if documentation is in sync"
    silent: true
    cmds:
      - cd docs-site && npm run docs:generate
      - |
        # Check if there are content changes (ignoring timestamp changes in frontmatter)
        DIFF_OUTPUT=$(git diff docs-site/content/_index.md docs-site/content/reference.md docs-site/content/services.md | grep -v "^[+-]date:" | grep -v "^[+-]lastmod:")
        if echo "$DIFF_OUTPUT" | grep -q "^[+-]"; then
          echo "❌ Documentation content out of sync. Run: cd docs-site && npm run docs:generate"
          echo "$DIFF_OUTPUT"
          exit 1
        fi
        echo "✅ Documentation is in sync"

  docs-lastmod-update:
    desc: "🗓️ Update lastmod dates for changed documentation files"
    silent: true
    cmds:
      - ./scripts/update-docs-lastmod.sh

  setup-hooks:
    desc: "🪝 Install Git hooks for pre-commit and pre-push checks"
    silent: true
    cmds:
      - ./scripts/setup-hooks.sh

  # ----------------------------------------------------------------------------
  # TESTING & QUALITY ASSURANCE
  # ----------------------------------------------------------------------------
  test:
    desc: "🧪 Run all tests with coverage report"
    deps: [deps]
    sources:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
    generates:
      - "coverage.out"
    silent: true
    cmds:
      - echo "🧪 Running tests with coverage..."
      - go test -v -race -coverprofile=coverage.out -covermode=atomic ./...
      - echo "✅ Coverage report at coverage.out"

  test-coverage:
    desc: "📊 View test coverage report in browser"
    deps: [test]
    silent: true
    cmds:
      - echo "📊 Opening coverage report..."
      - go tool cover -html=coverage.out

  test-coverage-check:
    desc: "🎯 Check if test coverage meets minimum threshold"
    deps: [test]
    silent: true
    cmds:
      - |
        COVERAGE=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | sed 's/%//')
        MIN_COVERAGE=15.0
        echo "Current test coverage: ${COVERAGE}%"
        echo "Minimum threshold: ${MIN_COVERAGE}%"

        if command -v bc >/dev/null 2>&1; then
          if (( $(echo "$COVERAGE < $MIN_COVERAGE" | bc -l) )); then
            echo "❌ Test coverage ${COVERAGE}% is below minimum threshold ${MIN_COVERAGE}%"
            exit 1
          else
            echo "✅ Test coverage ${COVERAGE}% meets minimum threshold ${MIN_COVERAGE}%"
          fi
        else
          # Fallback for systems without bc
          COVERAGE_INT=$(echo "$COVERAGE" | cut -d'.' -f1)
          MIN_COVERAGE_INT=$(echo "$MIN_COVERAGE" | cut -d'.' -f1)
          if [ "$COVERAGE_INT" -lt "$MIN_COVERAGE_INT" ]; then
            echo "❌ Test coverage ${COVERAGE}% is below minimum threshold ${MIN_COVERAGE}%"
            exit 1
          else
            echo "✅ Test coverage ${COVERAGE}% meets minimum threshold ${MIN_COVERAGE}%"
          fi
        fi

  test-unit:
    desc: "🧪 Run unit tests only"
    deps: [deps]
    silent: true
    cmds:
      - echo "🧪 Running unit tests..."
      - go test -race -short ./...
      - echo "✅ Unit tests passed"

  test-integration:
    desc: "🔗 Run integration tests"
    deps: [deps]
    silent: true
    cmds:
      - echo "🔗 Running integration tests..."
      - go test -race -tags=integration ./...
      - echo "✅ Integration tests passed"

  test-all:
    desc: "🧪 Run all test types"
    deps: [test-unit, test-integration]
    silent: true
    cmds:
      - echo "✅ All tests completed successfully"

  lint:
    desc: "🔍 Run comprehensive linting (vet + fmt + golangci-lint)"
    deps: [deps]
    silent: true
    cmds:
      - echo "🔍 Running go vet..."
      - go vet ./...
      - echo "🔍 Checking code formatting..."
      - |
        if [ "$(gofmt -l . | wc -l)" -gt 0 ]; then
          echo "❌ Code is not formatted. Run 'task fmt' to fix:"
          gofmt -l .
          exit 1
        fi
      - echo "✅ Basic linting passed"
      - echo "🔍 Running golangci-lint config verify..."
      - |
        if [ ! -f .golangci.yml ]; then
          echo "❌ .golangci.yml config file not found!"
          exit 1
        fi
        if [ ! -f $(go env GOPATH)/bin/golangci-lint ]; then
          echo "📦 Installing golangci-lint {{.GOLANGCI_LINT_VERSION}}..."
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin {{.GOLANGCI_LINT_VERSION}}
        fi
        $(go env GOPATH)/bin/golangci-lint config verify
      - echo "✅ golangci-lint config is valid"
      - echo "🔍 Running golangci-lint..."
      - $(go env GOPATH)/bin/golangci-lint run ./...
      - echo "✅ All linting checks passed!"

  fmt:
    desc: "✨ Format Go code with gofmt and goimports"
    silent: true
    cmds:
      - echo "✨ Formatting Go code..."
      - |
        if [ ! -f $(go env GOPATH)/bin/goimports ]; then
          echo "📦 Installing goimports..."
          go install golang.org/x/tools/cmd/goimports@latest
        fi
      - go fmt ./...
      - $(go env GOPATH)/bin/goimports -w .
      - echo "✅ Code formatted"

  security:
    desc: "🔒 Run security vulnerability scan"
    deps: [deps]
    silent: true
    cmds:
      - echo "🔒 Running security scan..."
      - |
        if ! command -v govulncheck >/dev/null 2>&1; then
          echo "📦 Installing govulncheck..."
          go install golang.org/x/vuln/cmd/govulncheck@latest
        fi
      - govulncheck ./...
      - echo "✅ Security scan complete"

  # ----------------------------------------------------------------------------
  # DOCUMENTATION
  # ----------------------------------------------------------------------------
  docs:
    desc: "📚 Generate documentation"
    deps: [build]
    silent: true
    cmds:
      - echo "📚 Generating documentation..."
      - ./{{.BUILD_DIR}}/{{.BINARY_NAME}} docs --verbose
      - echo "✅ Documentation generated"

  # ----------------------------------------------------------------------------
  # DOCKER
  # ----------------------------------------------------------------------------
  docker-build:
    desc: "🐳 Build Docker image"
    silent: true
    cmds:
      - echo "🐳 Building Docker image..."
      - docker build -t {{.BINARY_NAME}}:{{.VERSION}} .
      - echo "✅ Docker image built at {{.BINARY_NAME}}:{{.VERSION}}"

  docker-run:
    desc: "🐳 Run Docker container interactively"
    deps: [docker-build]
    silent: true
    cmds:
      - echo "🐳 Running Docker container..."
      - docker run --rm -it {{.BINARY_NAME}}:{{.VERSION}}

  # ----------------------------------------------------------------------------
  # CLEANUP
  # ----------------------------------------------------------------------------
  clean:
    desc: "🧹 Remove build artifacts and generated files"
    silent: true
    cmds:
      - echo "🧹 Cleaning build artifacts..."
      - rm -rf {{.BUILD_DIR}}/
      - rm -f coverage.out *.test *.prof
      - go clean -cache
      - echo "✅ Clean complete"

  clean-all:
    desc: "🧹 Deep clean - remove all generated files including dependencies"
    deps: [clean]
    silent: true
    cmds:
      - echo "🧹 Deep cleaning..."
      - go clean -modcache
      - rm -rf vendor/
      - echo "✅ Deep clean complete"

  # ----------------------------------------------------------------------------
  # UTILITY
  # ----------------------------------------------------------------------------
  version:
    desc: "ℹ️  Show version information"
    silent: true
    cmds:
      - echo "📋 Version Information:"
      - echo "   Version:" {{.VERSION}}
      - echo "   Commit:"  {{.COMMIT}}
      - echo "   Date:"    {{.BUILD_DATE}}

  help:
    desc: "❓ Show this help message"
    silent: true
    cmds:
      - echo "🛠️  dev-stack Task Runner"
      - echo ""
      - echo "📋 Quick Start:"
      - echo "   task build     - Build the binary"
      - echo "   task dev       - Build and run"
      - echo "   task test      - Run tests"
      - echo "   task lint      - Check code quality"
      - echo ""
      - echo "🔨 Build Tasks:"
      - echo "   build          - Build binary for current platform"
      - echo "   build-all      - Build for all platforms"
      - echo "   install        - Install to GOPATH/bin"
      - echo ""
      - echo "🚀 Development:"
      - echo "   dev            - Quick start (build + help)"
      - echo "   watch          - Auto-rebuild on changes"
      - echo ""
      - echo "🧪 Testing:"
      - echo "   test           - Run all tests"
      - echo "   test-coverage  - Run tests with coverage"
      - echo "   deps           - Update dependencies"
      - echo ""
      - echo "🧪 Testing & Quality:"
      - echo "   test           - Run tests with coverage"
      - echo "   test-unit      - Run unit tests only"
      - echo "   test-integration - Run integration tests"
      - echo "   test-all       - Run all test types"
      - echo "   test-coverage  - View coverage in browser"
      - echo "   test-coverage-check - Check coverage threshold"
      - echo "   lint           - Run all linting checks"
      - echo "   fmt            - Format code"
      - echo "   security       - Security vulnerability scan"
      - echo ""
      - echo "📚 Documentation:"
      - echo "   docs           - Generate documentation"
      - echo ""
      - echo "🐳 Docker:"
      - echo "   docker-build   - Build Docker image"
      - echo "   docker-run     - Run in container"
      - echo ""
      - echo "🧹 Cleanup:"
      - echo "   clean          - Remove build artifacts"
      - echo "   clean-all      - Deep clean everything"
      - echo ""
      - echo "ℹ️  Info:"
      - echo "   version        - Show version info"
      - echo "   help           - Show this help"
      - echo ""
      - echo "🤖 AI-Powered Git Automation:"
      - echo "   commit         - AI-generated commit messages"
      - echo "   create-pr      - AI-generated PR title/description"
      - echo "   push-and-pr    - Push branch and create AI PR"
      - echo "   pre-commit     - Run all pre-commit checks"
      - echo "   pre-push       - Run all pre-push checks"
      - echo "   setup-hooks    - Install Git hooks"
      - echo ""
      - echo "💡 Examples:"
      - echo "   task build && ./build/dev-stack --help"
      - echo "   task watch  (Auto-rebuild on file changes)"
      - echo "   task test && task lint"
      - echo ""
      - echo "For detailed task list:" "task --list"
